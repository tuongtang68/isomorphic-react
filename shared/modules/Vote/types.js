// @flow

export type SubmitVoteProps = {
  minScore: number,
  maxScore: number,
  scores: Array,
  submitVote: void
};

export type ScoresProps = {
  scores: Array,
  style: {},
  submitVote: void
}
